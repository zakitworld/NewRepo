<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineVoting_and_Ticketing_app.Views.Shared.OfflineBanner">

    <Border x:Name="BannerRoot"
            IsVisible="False"
            BackgroundColor="#DC2626"
            StrokeThickness="0"
            Padding="14,10">
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
            <Label Text="⚠" FontSize="14" TextColor="White" VerticalOptions="Center"/>
            <Label Text="No internet connection" FontSize="13" FontAttributes="Bold"
                   TextColor="White" VerticalOptions="Center"/>
        </HorizontalStackLayout>
    </Border>

</ContentView>
