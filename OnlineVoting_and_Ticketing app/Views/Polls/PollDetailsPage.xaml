<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineVoting_and_Ticketing_app.Views.Polls.PollDetailsPage"
             Title="Poll Details"
             BackgroundColor="{StaticResource Background}">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="25">

            <!-- Poll Header Card -->
            <Border Style="{StaticResource Border}" Padding="25">
                <VerticalStackLayout Spacing="20">
                    
                    <Grid ColumnDefinitions="Auto,*">
                        <Border x:Name="StatusBorder" StrokeShape="RoundRectangle 8" Padding="12,6" StrokeThickness="0">
                            <Label x:Name="StatusLabel" Text="Active" TextColor="White" FontSize="12" FontAttributes="Bold" />
                        </Border>
                        <Label x:Name="VoteCountLabel" Grid.Column="1" HorizontalOptions="End" VerticalOptions="Center"
                               TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                    </Grid>

                    <Label x:Name="TitleLabel" Style="{StaticResource Headline}" HorizontalOptions="Start" TextColor="{StaticResource White}"/>
                    <Label x:Name="DescriptionLabel" FontSize="15" LineHeight="1.6" TextColor="{StaticResource TextSecondary}"/>

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="15">
                        <Border WidthRequest="44" HeightRequest="44" StrokeShape="RoundRectangle 22" 
                                Background="{StaticResource Surface}" StrokeThickness="0">
                            <Label Text="👤" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20"/>
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                            <Label x:Name="CreatorLabel" TextColor="{StaticResource White}" FontSize="15" FontAttributes="Bold"/>
                            <Label Text="Poll Creator" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                        </VerticalStackLayout>
                    </Grid>

                    <Border Background="#10FFFFFF" Stroke="{StaticResource GlassBorderBrush}" StrokeShape="RoundRectangle 12" Padding="15">
                        <VerticalStackLayout Spacing="10">
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="📅" FontSize="18"/>
                                <Label x:Name="DurationLabel" TextColor="{StaticResource White}" FontSize="14" VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                            <HorizontalStackLayout Spacing="10">
                                <Label Text="⏰" FontSize="18"/>
                                <Label x:Name="TimeRemainingLabel" TextColor="{StaticResource Secondary}" FontSize="14" FontAttributes="Bold" VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Border>

                    <HorizontalStackLayout Spacing="10">
                        <Border x:Name="MultipleChoiceBadge" IsVisible="False" Background="#208B5CF6" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="10,5">
                            <Label Text="Multiple Choice" TextColor="#A78BFA" FontSize="11" FontAttributes="Bold" />
                        </Border>
                        <Border x:Name="AnonymousBadge" IsVisible="False" Background="#20F59E0B" StrokeThickness="0" StrokeShape="RoundRectangle 6" Padding="10,5">
                            <Label Text="Anonymous" TextColor="#FBBF24" FontSize="11" FontAttributes="Bold" />
                        </Border>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Voting Section -->
            <VerticalStackLayout x:Name="VotingSection" Spacing="20">
                <Label Text="Cast Your Vote" FontSize="20" FontAttributes="Bold" TextColor="{StaticResource White}" />
                <Label x:Name="VotingInstructionsLabel" TextColor="{StaticResource TextSecondary}" FontSize="14" />
                <VerticalStackLayout x:Name="OptionsContainer" Spacing="12" />
                <Button x:Name="SubmitVoteButton" Text="Submit Vote" Style="{StaticResource Button}" HeightRequest="56" Clicked="OnSubmitVoteClicked" />
            </VerticalStackLayout>

            <!-- Results Section -->
            <VerticalStackLayout x:Name="ResultsSection" Spacing="20" IsVisible="False">
                <Grid ColumnDefinitions="*,Auto">
                    <Label Text="Real-time Results" FontSize="20" FontAttributes="Bold" TextColor="{StaticResource White}" />
                    <Label x:Name="ResultsVoteCountLabel" Grid.Column="1" TextColor="{StaticResource TextSecondary}" FontSize="14" VerticalOptions="Center" />
                </Grid>
                <VerticalStackLayout x:Name="ResultsContainer" Spacing="15" />
                <Label x:Name="ThankYouLabel" Text="✓ Your vote has been recorded" Style="{StaticResource SubHeadline}" TextColor="{StaticResource Secondary}" IsVisible="False" HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Status Messages -->
            <Border x:Name="AlreadyVotedSection" IsVisible="False" Style="{StaticResource Border}" Background="#10F59E0B" Stroke="#30F59E0B" Padding="20">
                <HorizontalStackLayout Spacing="15">
                    <Label Text="✓" TextColor="#F59E0B" FontSize="24" VerticalOptions="Center"/>
                    <VerticalStackLayout VerticalOptions="Center">
                        <Label Text="Voice Recorded" TextColor="#FBBF24" FontSize="15" FontAttributes="Bold" />
                        <Label Text="You have already participated in this poll." TextColor="{StaticResource TextSecondary}" FontSize="12" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <Label x:Name="ErrorLabel" TextColor="#F43F5E" FontSize="14" IsVisible="False" HorizontalTextAlignment="Center" />
            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="False" IsVisible="False" Style="{StaticResource ActivityIndicator}" HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
