<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineVoting_and_Ticketing_app.Views.Events.EventsPage"
             Title="Discover Events"
             BackgroundColor="{StaticResource Background}">

    <Grid RowDefinitions="Auto,*">
        <!-- Background Glow -->
        <Ellipse Grid.RowSpan="2" WidthRequest="300" HeightRequest="300"
                 Fill="{StaticResource Primary}" Opacity="0.03"
                 VerticalOptions="Start" HorizontalOptions="End" Margin="0,-100,-100,0"/>

        <!-- ── Header: search + category chips ──────────────────────── -->
        <VerticalStackLayout Grid.Row="0" Padding="25" Spacing="20">
            <Label Text="Explore Events" Style="{StaticResource Headline}" TextColor="{StaticResource White}"/>

            <!-- Search Bar -->
            <Border Style="{StaticResource Border}" Padding="15,2" HeightRequest="50">
                <Grid ColumnDefinitions="Auto,*">
                    <Label Text="🔍" VerticalOptions="Center" Margin="0,0,10,0" Opacity="0.5"/>
                    <Entry x:Name="SearchEntry" Grid.Column="1"
                           Placeholder="Search events..."
                           PlaceholderColor="{StaticResource TextSecondary}"
                           TextColor="{StaticResource White}"
                           FontSize="14" VerticalOptions="Center"
                           TextChanged="OnSearchTextChanged"/>
                </Grid>
            </Border>

            <!-- Categories -->
            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout Spacing="10">
                    <Border x:Name="AllCategoryButton"
                            StrokeThickness="0" StrokeShape="RoundRectangle 25"
                            BackgroundColor="{StaticResource Primary}" Padding="20,10">
                        <Label Text="All" TextColor="White" FontSize="13" FontAttributes="Bold"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="All"/>
                        </Border.GestureRecognizers>
                    </Border>
                    <Border Style="{StaticResource Border}" StrokeShape="RoundRectangle 25" Padding="20,10">
                        <Label Text="Music" TextColor="{StaticResource White}" FontSize="13"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="Music"/>
                        </Border.GestureRecognizers>
                    </Border>
                    <Border Style="{StaticResource Border}" StrokeShape="RoundRectangle 25" Padding="20,10">
                        <Label Text="Sports" TextColor="{StaticResource White}" FontSize="13"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="Sports"/>
                        </Border.GestureRecognizers>
                    </Border>
                    <Border Style="{StaticResource Border}" StrokeShape="RoundRectangle 25" Padding="20,10">
                        <Label Text="Conference" TextColor="{StaticResource White}" FontSize="13"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="Conference"/>
                        </Border.GestureRecognizers>
                    </Border>
                    <Border Style="{StaticResource Border}" StrokeShape="RoundRectangle 25" Padding="20,10">
                        <Label Text="Workshop" TextColor="{StaticResource White}" FontSize="13"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="Workshop"/>
                        </Border.GestureRecognizers>
                    </Border>
                    <Border Style="{StaticResource Border}" StrokeShape="RoundRectangle 25" Padding="20,10">
                        <Label Text="Festival" TextColor="{StaticResource White}" FontSize="13"/>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCategoryTapped" CommandParameter="Festival"/>
                        </Border.GestureRecognizers>
                    </Border>
                </HorizontalStackLayout>
            </ScrollView>
        </VerticalStackLayout>

        <!-- ── Main content (Row 1) ──────────────────────────────────── -->
        <Grid Grid.Row="1" RowDefinitions="Auto,*">

            <!-- Trending / Featured carousel strip -->
            <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,0,0,8"
                                 x:Name="TrendingSection">
                <Grid ColumnDefinitions="*,Auto" Padding="25,4,25,0">
                    <Label Text="Trending Now 🔥"
                           FontSize="16" FontAttributes="Bold"
                           TextColor="{StaticResource White}"
                           VerticalOptions="Center"/>
                </Grid>

                <!-- Horizontal featured cards -->
                <CollectionView x:Name="TrendingCarousel"
                                ItemsSource="{Binding FeaturedEvents}"
                                HeightRequest="160"
                                SelectionMode="Single"
                                SelectionChanged="OnTrendingEventSelected">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal" ItemSpacing="12"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeThickness="0"
                                    StrokeShape="RoundRectangle 14"
                                    WidthRequest="160"
                                    HeightRequest="150"
                                    Padding="0"
                                    Margin="0">
                                <Grid>
                                    <Image Source="{Binding ImageUrl}"
                                           Aspect="AspectFill"
                                           HeightRequest="150"/>
                                    <Rectangle HeightRequest="150">
                                        <Rectangle.Fill>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="Transparent" Offset="0.4"/>
                                                <GradientStop Color="#DD0A0A0C"   Offset="1.0"/>
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                    <VerticalStackLayout VerticalOptions="End" Padding="10,8" Spacing="3">
                                        <Label Text="{Binding Title}"
                                               TextColor="White"
                                               FontSize="13"
                                               FontAttributes="Bold"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="2"/>
                                        <Label Text="{Binding StartDate, StringFormat='{0:MMM dd}'}"
                                               TextColor="{StaticResource Primary}"
                                               FontSize="11"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Thin divider -->
                <BoxView HeightRequest="1"
                         BackgroundColor="{StaticResource GlassBorderBrush}"
                         Margin="25,0"/>
            </VerticalStackLayout>

            <!-- Full event list -->
            <RefreshView Grid.Row="1" x:Name="RefreshView"
                         Refreshing="OnRefreshing"
                         RefreshColor="{StaticResource Primary}">
                <CollectionView x:Name="EventsCollectionView"
                                ItemsSource="{Binding Events}"
                                SelectionMode="Single"
                                SelectionChanged="OnEventSelected"
                                Margin="20,0"
                                RemainingItemsThreshold="3"
                                RemainingItemsThresholdReached="OnLoadMore">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.Header>
                        <Label Text="All Events"
                               FontSize="15" FontAttributes="Bold"
                               TextColor="{StaticResource TextSecondary}"
                               Margin="0,10,0,4"/>
                    </CollectionView.Header>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Border}" Padding="0">
                                <Grid RowDefinitions="180,Auto">
                                    <!-- Image -->
                                    <Grid Grid.Row="0">
                                        <Image Source="{Binding ImageUrl}" Aspect="AspectFill"/>
                                        <Rectangle Fill="{StaticResource Background}" Opacity="0.2"/>
                                        <Border HorizontalOptions="End" VerticalOptions="Top"
                                                Margin="15" Padding="10,5"
                                                StrokeShape="RoundRectangle 8"
                                                Background="#CC16161A"
                                                StrokeThickness="0">
                                            <Label Text="{Binding Category}"
                                                   TextColor="{StaticResource Primary}"
                                                   FontSize="9" FontAttributes="Bold"
                                                   CharacterSpacing="1"/>
                                        </Border>
                                    </Grid>
                                    <!-- Details -->
                                    <VerticalStackLayout Grid.Row="1" Padding="20" Spacing="12">
                                        <Label Text="{Binding Title}"
                                               FontSize="18" FontAttributes="Bold"
                                               TextColor="{StaticResource White}"/>
                                        <HorizontalStackLayout Spacing="20">
                                            <HorizontalStackLayout Spacing="6">
                                                <Label Text="📅" FontSize="12" VerticalOptions="Center" Opacity="0.7"/>
                                                <Label Text="{Binding StartDate, StringFormat='{0:ddd, MMM dd}'}"
                                                       TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                            </HorizontalStackLayout>
                                            <HorizontalStackLayout Spacing="6">
                                                <Label Text="📍" FontSize="12" VerticalOptions="Center" Opacity="0.7"/>
                                                <Label Text="{Binding Location}"
                                                       TextColor="{StaticResource TextSecondary}"
                                                       FontSize="12" LineBreakMode="TailTruncation"/>
                                            </HorizontalStackLayout>
                                        </HorizontalStackLayout>
                                        <Grid ColumnDefinitions="*,Auto" Margin="0,5,0,0">
                                            <HorizontalStackLayout Spacing="4">
                                                <Label Text="From" FontSize="11"
                                                       TextColor="{StaticResource TextSecondary}"
                                                       VerticalOptions="End" Margin="0,0,0,2"/>
                                                <Label Text="{Binding TicketTypes[0].Price, StringFormat='GH₵ {0:N0}'}"
                                                       TextColor="{StaticResource Primary}"
                                                       FontSize="18" FontAttributes="Bold"/>
                                            </HorizontalStackLayout>
                                            <Label Grid.Column="1"
                                                   Text="View Details →"
                                                   TextColor="{StaticResource Secondary}"
                                                   FontSize="12" VerticalOptions="Center"
                                                   FontAttributes="Bold"/>
                                        </Grid>
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.Footer>
                        <ActivityIndicator x:Name="LoadMoreIndicator"
                                           IsRunning="{Binding IsLoadingMore}"
                                           IsVisible="{Binding IsLoadingMore}"
                                           Color="{StaticResource Primary}"
                                           Margin="0,10,0,20"/>
                    </CollectionView.Footer>

                    <CollectionView.EmptyView>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center"
                                             Spacing="25" Padding="40" Margin="0,50,0,0">
                            <Border WidthRequest="100" HeightRequest="100"
                                    StrokeShape="RoundRectangle 50"
                                    BackgroundColor="{StaticResource Surface}"
                                    Stroke="{StaticResource GlassBorderBrush}">
                                <Label Text="✨" FontSize="40"
                                       HorizontalOptions="Center" VerticalOptions="Center"
                                       Opacity="0.5"/>
                            </Border>
                            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                                <Label Text="No Events Found"
                                       Style="{StaticResource Headline}"
                                       FontSize="20" HorizontalOptions="Center"
                                       TextColor="{StaticResource White}"/>
                                <Label Text="Be the first to create history."
                                       FontSize="14"
                                       TextColor="{StaticResource TextSecondary}"
                                       HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
            </RefreshView>

        </Grid>

        <!-- Floating Action Button -->
        <Border Grid.Row="1"
                StrokeShape="RoundRectangle 28" StrokeThickness="0"
                BackgroundColor="{StaticResource Primary}"
                WidthRequest="56" HeightRequest="56"
                HorizontalOptions="End" VerticalOptions="End"
                Margin="25" Shadow="{StaticResource PrimaryShadow}">
            <Label Text="+" FontSize="30" TextColor="White"
                   HorizontalOptions="Center" VerticalOptions="Center"/>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnCreateEventTapped"/>
            </Border.GestureRecognizers>
        </Border>

        <ActivityIndicator Grid.Row="1" x:Name="LoadingIndicator"
                           IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Color="{StaticResource Primary}"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>
</ContentPage>
