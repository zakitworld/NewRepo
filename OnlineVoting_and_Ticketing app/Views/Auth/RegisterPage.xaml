<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineVoting_and_Ticketing_app.Views.Auth.RegisterPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Background}">

    <Grid>
        <!-- Background Decoration -->
        <Ellipse WidthRequest="300" HeightRequest="300" 
                 Fill="{StaticResource Secondary}" 
                 Opacity="0.1" VerticalOptions="End" 
                 HorizontalOptions="Start" Margin="-100,0,0,-100">
            <Ellipse.Shadow>
                <Shadow Brush="{StaticResource Secondary}" Radius="100" Opacity="0.5"/>
            </Ellipse.Shadow>
        </Ellipse>

        <ScrollView VerticalOptions="Fill">
            <VerticalStackLayout Padding="30,60,30,30" Spacing="0">
                
                <!-- Header -->
                <VerticalStackLayout Spacing="10" Margin="0,0,0,40">
                    <Label Text="Create Account" 
                           Style="{StaticResource Headline}"
                           TextColor="{StaticResource White}"/>
                    <Label Text="Join EventHub and start your journey" 
                           Style="{StaticResource SubHeadline}"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <!-- Glass Card -->
                <Border Style="{StaticResource Border}" Padding="25,35" Margin="0,0,0,20">
                    <VerticalStackLayout Spacing="20">
                        
                        <!-- Input Fields -->
                        <VerticalStackLayout Spacing="15">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="Full Name" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                <Border Stroke="{StaticResource GlassBorderBrush}" 
                                        StrokeShape="RoundRectangle 8" 
                                        Background="#10FFFFFF" 
                                        Padding="12,2">
                                    <Entry x:Name="FullNameEntry" 
                                           Placeholder="John Doe" 
                                           Style="{StaticResource Entry}"/>
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Spacing="8">
                                <Label Text="Email Address" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                <Border Stroke="{StaticResource GlassBorderBrush}" 
                                        StrokeShape="RoundRectangle 8" 
                                        Background="#10FFFFFF" 
                                        Padding="12,2">
                                    <Entry x:Name="EmailEntry" 
                                           Placeholder="name@example.com" 
                                           Keyboard="Email"
                                           Style="{StaticResource Entry}"/>
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Spacing="8">
                                <Label Text="Password" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                <Border Stroke="{StaticResource GlassBorderBrush}" 
                                        StrokeShape="RoundRectangle 8" 
                                        Background="#10FFFFFF" 
                                        Padding="12,2">
                                    <Entry x:Name="PasswordEntry" 
                                           Placeholder="••••••••" 
                                           IsPassword="True"
                                           Style="{StaticResource Entry}"/>
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Spacing="8">
                                <Label Text="Confirm Password" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                <Border Stroke="{StaticResource GlassBorderBrush}" 
                                        StrokeShape="RoundRectangle 8" 
                                        Background="#10FFFFFF" 
                                        Padding="12,2">
                                    <Entry x:Name="ConfirmPasswordEntry" 
                                           Placeholder="••••••••" 
                                           IsPassword="True"
                                           Style="{StaticResource Entry}"/>
                                </Border>
                            </VerticalStackLayout>
                        </VerticalStackLayout>

                        <Button x:Name="RegisterButton" 
                                Text="Sign Up" 
                                Clicked="OnRegisterClicked" 
                                Style="{StaticResource Button}"
                                HeightRequest="56"
                                Margin="0,10,0,0"/>

                        <Label x:Name="ErrorLabel" 
                               TextColor="{StaticResource Tertiary}" 
                               FontSize="13" 
                               IsVisible="False" 
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Border>

                <!-- Alternative Sign Up -->
                <VerticalStackLayout Spacing="20" HorizontalOptions="Center">
                    <Label Text="Or sign up with" 
                           TextColor="{StaticResource TextSecondary}" 
                           HorizontalOptions="Center"/>
                    
                    <HorizontalStackLayout Spacing="15" HorizontalOptions="Center">
                        <Border Style="{StaticResource Border}" Padding="25,12" WidthRequest="120">
                            <Label Text="Google" HorizontalOptions="Center" TextColor="{StaticResource White}"/>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnGoogleSignUpTapped"/>
                            </Border.GestureRecognizers>
                        </Border>
                        <Border Style="{StaticResource Border}" Padding="25,12" WidthRequest="120">
                            <Label Text="Facebook" HorizontalOptions="Center" TextColor="{StaticResource White}"/>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnFacebookSignUpTapped"/>
                            </Border.GestureRecognizers>
                        </Border>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center" Margin="0,20,0,20">
                        <Label Text="Already high on EventHub?" TextColor="{StaticResource TextSecondary}"/>
                        <Label Text="Sign In" 
                               TextColor="{StaticResource Primary}" 
                               FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSignInTapped"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
